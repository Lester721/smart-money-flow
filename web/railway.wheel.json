{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'forward-wheel: sin build (tsx corre TypeScript directo)'"
  },
  "deploy": {
    "startCommand": "node scripts/with-theta.mjs npm run forward-wheel",
    "restartPolicyType": "NEVER"
  }
}