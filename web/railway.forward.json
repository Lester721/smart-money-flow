{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'forward-test: sin build (tsx corre TypeScript directo)'"
  },
  "deploy": {
    "startCommand": "npm run forward-test",
    "restartPolicyType": "NEVER"
  }
}
