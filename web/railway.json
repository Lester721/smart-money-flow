{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'worker: sin build (tsx corre TypeScript directo)'"
  },
  "deploy": {
    "startCommand": "npm run worker",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10
  }
}
