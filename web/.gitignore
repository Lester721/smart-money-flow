/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# secrets — never commit
.env
.env.local
.env*.local

# historial local de transacciones categorizadas
/data

# Caché de datos del backtest (grande, no versionar)
scripts/cache*/

# Key de ThetaData (la usa el Terminal, nunca versionar)
.env.thetadata

# Jar del Theta Terminal (se descarga solo, 40MB)
ThetaTerminalv3.jar
config.toml
